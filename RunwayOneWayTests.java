import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;
import java.util.*;

public class RunwayOneWayTests
{ /*
    @Test
    public void createRunwayBoundary()
    {
        try {
            ObstacleData od = new ObstacleData(100, 20);
            RunwayOneWay rw = new RunwayOneWay("TEST", new RunwayData(100, 1000, 100, 100));
            assertTrue(rw.getName().equals("TEST"));
            assertTrue(BackendHelpers.runwayDataEqual(rw.getRunwaySpec(), new RunwayData(100, 1000, 100, 100)));
            assertTrue(BackendHelpers.runwayDataEqual(rw.getRunwaySpec(), rw.getUpdatedRunway()));
            rw.addObstacle(od);
            assertFalse(BackendHelpers.runwayDataEqual(rw.getRunwaySpec(), rw.getUpdatedRunway()));
            assertTrue(rw.getObstacles().get(0) == od);
        } catch (Exception e) {
            assertTrue(false);
        }
    } */
}
